all:
  children:
    dev_group_name:
      hosts:
        #ip
      vars:
        admin_user: username
        ansible_python_interpreter: /usr/bin/python3
    deploy_group_name:
      hosts:
        #ip
      vars:
        admin_user: username
        ansible_python_interpreter: /usr/bin/python3
