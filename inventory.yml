all:
  children:
    dev_group_name:
      hosts:
        0.0.0.0
      vars:
        admin_user: username
        ansible_python_interpreter: /usr/bin/python
    deploy_group_name:
      hosts:
        0.0.0.0
      vars:
        admin_user: username
        ansible_python_interpreter: /usr/bin/python
